---
sidebar_position: 3
---

# Basic Usage


### Pass Client ID for verification

You need to pass a `clientId` prop to `DyteMeeting` to verify the participant's
`authToken` against.

```jsx {2}
<DyteMeeting
  clientId=`yourClientID`
>
```
